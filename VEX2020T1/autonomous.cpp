#include "main.h"

void autonomous() {
  screen.setActiveTab(TAB_DISPLAY);
  screen.refresh();
/*
  if(currentAuto == NO_AUTO_SELECTED){
    base.MoveDistance(DIRECTION_FORWARD, 300, 750);
    base.stopDriving();
    delay(700);
//  base.TurnDistance(DIRECTION_BACKWARD, 350, 1200);
    base.TurnGyro(DIRECTION_FORWARD, 50, 675);
    delay(200);
    base.stopDriving();
  }*/
}
